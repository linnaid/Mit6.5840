
LabA:
当时写的时候我认为思路没错，但是测试点就是过不去，在解决了一些关于锁和竞态的问题后，发现还是过不去，于是从头开始观察逻辑，发现在调用rpc进行投票时，给小任期进行了更新，但未将返回的reply进行更新，将此问题修改后，测试点通过...

LabB：
被一个测试点一直折磨，后面发现时因为投票选Leader的时候值保留了3A的部分，没有加入日志的考虑，改了之后已通过......

LabC:
在写LabC的时候，对之前的变量修改没处理到位，导致只写了两个函数时无法通过测试

LabD:
LabD卡了有一段时间，后面与之前的代码也对比过，一直找不到问题所在，直到一步一步精看代码的时候，发现自己没有在InstallSnapshot函数内向应用层发送状态，添加后通过测试
(以后不要再犯这种错误了)